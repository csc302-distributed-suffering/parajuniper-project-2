{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-app": "concurrently \"cd server && npm start\" \"cd frontend && npm start\"",
    "start-frontend": "cd frontend && npm start",
    "start-server": "cd server && npm start",
    "init-setup": "npm install && concurrently \"cd frontend && npm install\" \"cd server && npm install\"",
    "init-setup-run": "npm run init-setup && npm run start-app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.2",
    "fhir-kit-client": "^1.7.2"
  }
}
